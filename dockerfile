# Указываем базовый образ Go
FROM golang:1.23.5

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем все файлы из текущей папки внутрь контейнера
COPY . .

# Устанавливаем зависимости (go mod tidy подгрузит всё, что надо)
RUN go mod tidy

# Собираем бинарный файл бота
RUN go build -o bot

# Указываем, что будет выполняться при запуске контейнера
CMD ["./bot"]
